import React, {useState} from 'react'; import Login from './pages/Login.jsx'; import Dashboard from './pages/Dashboard.jsx'; export default function App(){ const [token,setToken]=useState(localStorage.getItem('token')||null); if(!token) return <Login onLogin={(t)=>{ setToken(t); localStorage.setItem('token',t); }} />; return <Dashboard onLogout={()=>{ localStorage.removeItem('token'); setToken(null); }} />; }