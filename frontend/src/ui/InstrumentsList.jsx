import React,{useEffect,useState} from 'react'; import axios from 'axios'; export default function InstrumentsList(){ const [list,setList]=useState([]); useEffect(()=>{ axios.get('/api/instruments').then(r=>setList(r.data)).catch(()=>{}); },[]); return (<ul style={{listStyle:'none',padding:0}}>{list.map(i=>(<li key={i.code} style={{padding:8,background:'#021427',marginBottom:8,borderRadius:6}}><div style={{fontWeight:700}}>{i.code}</div><div style={{fontSize:12,color:'#9ca3af'}}>{i.name}</div></li>))}</ul>); }